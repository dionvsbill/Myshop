"use client";
import { useState } from "react";
import { X, Save } from "lucide-react";
import { createClient } from "../lib/supabase/client";
export default function EditProductModal({product}:{product:{id:string;title:string;price:number;stock:number;is_active:boolean}}){const[open,setOpen]=useState(false),[saving,setSaving]=useState(false),[form,setForm]=useState({title:product.title,price:String(product.price),stock:String(product.stock),is_active:product.is_active});async function save(){setSaving(true);const s=createClient();const{error}=await s.from("products").update({title:form.title,price:Number(form.price),stock:Number(form.stock),is_active:form.is_active}).eq("id",product.id);setSaving(false);if(!error){setOpen(false);location.reload()}else alert(error.message)}return <><button onClick={()=>setOpen(true)} className="rounded-md border px-3 py-2 text-xs font-bold hover:border-[#f68b1e] hover:text-[#f68b1e]">Edit</button>{open&&<div className="fixed inset-0 z-[100] grid place-items-center bg-black/50 p-4" onMouseDown={e=>{if(e.target===e.currentTarget)setOpen(false)}}><div className="w-full max-w-lg rounded-xl bg-white shadow-2xl"><div className="flex items-center justify-between border-b px-5 py-4"><h2 className="font-bold">Edit product</h2><button onClick={()=>setOpen(false)}><X/></button></div><div className="grid gap-4 p-5"><label className="text-sm font-semibold">Product name<input value={form.title} onChange={e=>setForm({...form,title:e.target.value})} className="store-input mt-1"/></label><div className="grid grid-cols-2 gap-4"><label className="text-sm font-semibold">Price<input type="number" value={form.price} onChange={e=>setForm({...form,price:e.target.value})} className="store-input mt-1"/></label><label className="text-sm font-semibold">Stock<input type="number" value={form.stock} onChange={e=>setForm({...form,stock:e.target.value})} className="store-input mt-1"/></label></div><label className="flex items-center gap-3 text-sm font-semibold"><input type="checkbox" checked={form.is_active} onChange={e=>setForm({...form,is_active:e.target.checked})}/> Published</label></div><div className="flex justify-end gap-3 border-t px-5 py-4"><button onClick={()=>setOpen(false)} className="btn-secondary">Cancel</button><button disabled={saving} onClick={save} className="btn-primary"><Save size={16}/>{saving?"Saving...":"Save changes"}</button></div></div></div>}</>}