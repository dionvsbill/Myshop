"use client"; import Link from "next/link"; import {Menu,Search,ShoppingCart,User,LogOut} from "lucide-react"; import {createClient} from "@/lib/supabase/client"; import {useEffect,useState} from "react"; export default function Header(){const [user,setUser]=useState<any>(null);const [cart,setCart]=useState(0);useEffect(()=>{const s=createClient();s.auth.getUser().then(({data})=>setUser(data.user)); if(user)s.from("cart_items").select("id",{count:"exact",head:true}).then(r=>setCart(r.count||0));},[user]);return <header className="sticky top-0 z-50 border-b bg-white"><div className="container flex h-16 items-center justify-between gap-4"><div className="flex items-center gap-3"><Menu className="md:hidden"/><Link href="/" className="text-xl font-bold">Myshop</Link><nav className="hidden gap-5 md:flex"><Link href="/products">Products</Link><Link href="/products">Categories</Link><Link href="/products">Deals</Link></nav></div><form action="/products" className="hidden md:flex w-96"><div className="flex w-full items-center rounded-lg border px-3"><Search size={17}/><input name="q" className="w-full p-2 outline-none" placeholder="Search products"/></div></form><div className="flex items-center gap-4"><Link href="/cart" className="relative"><ShoppingCart size={20}/>{cart>0&&<span className="absolute -right-2 -top-2 rounded-full bg-red-600 px-1.5 text-xs text-white">{cart}</span>}</Link>{user?<><Link href="/account"><User size={20}/></Link><button onClick={async()=>{await createClient().auth.signOut();location.href="/"}}><LogOut size={20}/></button></>:<Link href="/login">Login</Link>}</div></div></header>}