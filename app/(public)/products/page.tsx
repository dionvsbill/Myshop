import Link from "next/link"; import {Search,ShoppingCart,Star,PackageOpen} from "lucide-react"; import {createClient} from "../../../lib/supabase/server"; export default async function Products({searchParams}:{searchParams:{q?:string;category?:string}}){const s=await createClient();let q=s.from("products").select("*").eq("is_active",true).order("created_at",{ascending:false});if(searchParams.q)q=q.ilike("title","%"+searchParams.q+"%");const {data}=await q;return <section><div className="flex items-center justify-between"><h1 className="text-3xl font-bold">Products</h1><span>{data?.length||0} items</span></div><div className="mt-8 grid grid-cols-2 gap-4 md:grid-cols-3 lg:grid-cols-4">{(data||[]).map(p=><article key={p.id} className="rounded-xl border bg-white p-3 shadow-sm"><Link href={"/product/"+p.slug}><div className="flex h-48 items-center justify-center rounded-lg bg-slate-100">{p.images?.[0]?<img src={p.images[0]} alt="" className="h-full w-full rounded-lg object-cover"/>:<PackageOpen className="text-slate-300"/>}</div><h2 className="mt-3 line-clamp-2 font-medium">{p.title}</h2><div className="mt-2 flex items-center gap-1 text-sm"><Star size={15} className="fill-current"/> {Number(p.rating||0).toFixed(1)} ({p.review_count||0})</div><p className="mt-2 text-lg font-bold">GHS {Number(p.price).toFixed(2)}</p></Link><form action="/api/cart" method="post" className="mt-3"><input type="hidden" name="product_id" value={p.id}/><button className="flex w-full items-center justify-center gap-2 rounded-lg bg-slate-950 py-2 text-white"><ShoppingCart size={17}/>Add to Cart</button></form></article>)}</div></section>}