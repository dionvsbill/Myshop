"use client";

import { useEffect, useState } from "react";
import { Star, PackageOpen } from "lucide-react";
import ProductOptions from "../../../../components/ProductOptions";
import { createClient } from "../../../../lib/supabase/client";

export default function Product({ params }: { params: { slug: string } }) {
  const [p,setP]=useState<any>(null); const [loading,setLoading]=useState(true);
  useEffect(()=>{ const load=async()=>{ const s=createClient(); const {data}=await s.from("products").select("*,categories(name),product_options(id,name,position,product_option_values(id,value,swatch_hex,position)),product_variants(id,price,stock,option_values,image_url,is_active),product_media(id,url,alt_text,position,is_featured)").eq("slug",params.slug).eq("is_active",true).single(); setP(data); setLoading(false); }; load(); },[params.slug]);
  if(loading) return <section className="grid gap-10 md:grid-cols-2"><div className="h-[520px] animate-pulse rounded-2xl bg-slate-200"/><div className="space-y-4"><div className="h-8 w-2/3 animate-pulse rounded bg-slate-200"/><div className="h-24 animate-pulse rounded bg-slate-200"/></div></section>;
  if(!p) return <section className="py-20 text-center"><PackageOpen className="mx-auto h-16 w-16 text-slate-300"/><h1 className="mt-4 text-2xl font-bold">Product not found</h1></section>;
  const media=(p.product_media||[]).sort((a:any,b:any)=>a.position-b.position); const options=(p.product_options||[]).sort((a:any,b:any)=>a.position-b.position).map((o:any)=>({...o,values:(o.product_option_values||[]).sort((a:any,b:any)=>a.position-b.position)})); const variants=(p.product_variants||[]).filter((v:any)=>v.is_active);
  return <section><div className="grid gap-10 md:grid-cols-2"><div><div className="flex min-h-[420px] items-center justify-center overflow-hidden rounded-2xl bg-white p-4 shadow-sm">{media[0]?.url||p.images?.[0]?<img src={media[0]?.url||p.images[0]} alt={media[0]?.alt_text||p.title} className="max-h-[520px] w-full object-contain"/>:<PackageOpen className="h-20 w-20 text-slate-300"/>}</div>{media.length>1&&<div className="mt-3 grid grid-cols-4 gap-2">{media.map((m:any)=><img key={m.id} src={m.url} alt={m.alt_text||p.title} className="h-24 w-full rounded-lg border object-cover"/>)}</div>}</div><div><p className="text-sm text-slate-500">{p.categories?.name}{p.brand?" · "+p.brand:""}</p><h1 className="mt-2 text-3xl font-bold">{p.title}</h1><div className="mt-3 flex gap-1"><Star className="fill-current"/>{Number(p.rating||0).toFixed(1)} ({p.review_count||0})</div><p className="mt-5 text-3xl font-bold">GHS {Number(p.price).toFixed(2)}</p>{p.compare_price&&<p className="text-sm text-slate-500 line-through">GHS {Number(p.compare_price).toFixed(2)}</p>}<p className="mt-6 whitespace-pre-wrap text-slate-600">{p.description}</p>{p.features?.length>0&&<div className="mt-6"><h2 className="font-semibold">Highlights</h2><ul className="mt-2 list-disc space-y-1 pl-5 text-sm text-slate-600">{p.features.map((x:string)=><li key={x}>{x}</li>)}</ul></div>}<ProductOptions productId={p.id} options={options} variants={variants} fallbackPrice={Number(p.price)} fallbackStock={p.stock}/>{p.specifications&&<div className="mt-8 rounded-xl border p-4"><h2 className="font-semibold">Specifications</h2><dl className="mt-3 space-y-2 text-sm">{Object.entries(p.specifications).map(([k,v])=><div key={k} className="flex justify-between gap-4 border-b py-2"><dt className="text-slate-500">{k}</dt><dd className="font-medium text-right">{String(v)}</dd></div>)}</dl></div>}</div></div></section>;
}