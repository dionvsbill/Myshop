"use client";
import Link from "next/link";import {useState} from "react";import {ShieldCheck,ShoppingBag,Truck} from "lucide-react";import {createClient} from "../../../lib/supabase/client";import styles from "../../../components/auth.module.css";
export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setError("");const{error}=await createClient().auth.signInWithPassword({email,password});if(error)setError(error.message);else location.href="/"}async function google(){const s=createClient();const{error}=await s.auth.signInWithOAuth({provider:"google",options:{redirectTo:location.origin+"/auth/callback"}});if(error)setError(error.message)}return <main className={styles.page}><div className={styles.shell}><section className={styles.visual}><div className={styles.brand}>MY<span className={styles.accent}>SHOP</span></div><h2>Everything you need, in one place.</h2><p>Discover products, follow your orders and shop securely from any device.</p><div className={styles.benefits}><div className={styles.benefit}><ShoppingBag size={15}/> Easy shopping and order management</div><div className={styles.benefit}><ShieldCheck size={15}/> Secure account and checkout</div><div className={styles.benefit}><Truck size={15}/> Order tracking from your account</div></div></section><section className={styles.formSide}><h1 className={styles.heading}>Welcome back</h1><p className={styles.sub}>Sign in to continue shopping.</p><form onSubmit={submit} className={styles.form}><label className={styles.label}>Email<input className={styles.input} type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label className={styles.label}>Password<input className={styles.input} type="password" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="Your password"/></label>{error&&<p className={styles.error}>{error}</p>}<button className={styles.submit}>Sign in</button><button type="button" onClick={google} className={styles.google}>Continue with Google</button><p className={styles.foot}><Link href="/forgot-password">Forgot password?</Link></p><p className={styles.foot}>New to Myshop? <Link href="/register">Create an account</Link></p></form></section></div></main>}