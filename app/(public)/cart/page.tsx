import Link from "next/link";import {ShoppingBag,ArrowRight,ShieldCheck} from "lucide-react";import CartActions from "../../../components/CartActions";import {createClient} from "../../../lib/supabase/server";import styles from "./cart.module.css";
export default async function Cart(){const s=await createClient();const{data:user}=await s.auth.getUser();if(!user.user)return <div className={styles.empty}><ShoppingBag size={44}/><h1>Sign in to use your cart</h1><Link href="/login" className={styles.primary}>Sign in</Link></div>;const{data}=await s.from("cart_items").select("id,quantity,variant_id,products(id,title,price,images,stock),product_variants(id,title,price,stock,option_values,image_url)").eq("user_id",user.user.id);const items=data||[];const total=items.reduce((n:any,i:any)=>n+Number(i.product_variants?.price??i.products?.price??0)*i.quantity,0);return <section className={styles.wrap}><div className={styles.heading}><div><span className={styles.eyebrow}>MYSHOP</span><h1>Shopping cart</h1><p>{items.length} {items.length===1?"item":"items"} ready for checkout</p></div></div>{!items.length?<div className={styles.emptyCard}><ShoppingBag size={42}/><h2>Your cart is empty</h2><p>Find something you love and add it here.</p><Link href="/products" className={styles.primary}>Continue shopping</Link></div>:<div className={styles.grid}><div className={styles.items}>{items.map((i:any)=>{const v=i.product_variants,price=Number(v?.price??i.products?.price??0);return <article key={i.id} className={styles.item}><div className={styles.image}>{(v?.image_url||i.products?.images?.[0])&&<img src={v?.image_url||i.products.images[0]} alt={i.products?.title}/>}</div><div className={styles.info}><Link href={"/product/"+(i.products?.id||"")} className={styles.title}>{i.products?.title}</Link>{v?.title&&<p className={styles.variant}>{v.title}</p>}<div className={styles.price}>GHS {price.toFixed(2)}</div><CartActions itemId={i.id} productId={i.products?.id} variantId={i.variant_id} quantity={i.quantity} stock={v?.stock??i.products?.stock??0}/></div></article>})}</div><aside className={styles.summary}><div className={styles.summaryHead}><h2>Order summary</h2><span>{items.length}</span></div><div className={styles.line}><span>Subtotal</span><b>GHS {total.toFixed(2)}</b></div><div className={styles.line}><span>Delivery</span><span>Calculated at checkout</span></div><div className={styles.total}><span>Total</span><b>GHS {total.toFixed(2)}</b></div><Link href="/checkout" className={styles.checkout}>Proceed to checkout <ArrowRight size={17}/></Link><div className={styles.secure}><ShieldCheck size={16}/> Secure Paystack payment</div></aside></div>}</section>}