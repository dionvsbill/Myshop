import { redirect } from "next/navigation";
import Link from "next/link";
import { BarChart3, ClipboardList, FolderTree, LayoutDashboard, Package, Settings, ShoppingBag, Users, Store } from "lucide-react";
import { createClient } from "../../lib/supabase/server";
const links=[["/admin","Home",LayoutDashboard],["/admin/products","Products",Package],["/admin/categories","Collections",FolderTree],["/admin/orders","Orders",ClipboardList],["/admin/users","Customers",Users],["/admin/analytics","Analytics",BarChart3],["/admin/settings","Settings",Settings]] as const;
export default async function AdminLayout({children}:{children:React.ReactNode}){const s=await createClient();const{data:u}=await s.auth.getUser();if(!u.user)redirect("/login");const{data:p}=await s.from("profiles").select("full_name,email,role").eq("id",u.user.id).single();if(p?.role!=="ADMIN")redirect("/");return <div className="min-h-screen bg-[#f5f6f8] md:pl-[272px]"><aside className="fixed inset-y-0 left-0 z-40 hidden w-[272px] border-r border-zinc-800 bg-zinc-950 text-white md:flex md:flex-col"><div className="border-b border-white/10 px-6 py-5"><Link href="/" className="text-xl font-black tracking-[-.05em]">MYSHOP<span className="text-white/30">.</span></Link><div className="mt-3 inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/5 px-3 py-1.5 text-[10px] font-bold uppercase tracking-[.16em] text-white/50"><Store size={12}/> Store admin</div></div><nav className="flex-1 space-y-1 px-3 py-5">{links.map(([href,label,Icon])=><Link key={href} href={href} className="group flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-semibold text-white/55 hover:bg-white/10 hover:text-white"><Icon size={18}/><span>{label}</span></Link>)}</nav><div className="border-t border-white/10 p-4"><div className="flex items-center gap-3 rounded-2xl bg-white/5 p-3"><div className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-white text-xs font-black text-zinc-950">{(p?.full_name||p?.email||"A").slice(0,1).toUpperCase()}</div><div className="min-w-0"><p className="truncate text-sm font-bold">{p?.full_name||"Administrator"}</p><p className="truncate text-xs text-white/40">{p?.email}</p></div></div></div></aside><header className="sticky top-0 z-30 flex h-[72px] items-center justify-between border-b border-zinc-200 bg-white/90 px-4 shadow-[0_4px_18px_rgba(0,0,0,.03)] backdrop-blur-xl md:px-8"><div><p className="text-[10px] font-bold uppercase tracking-[.18em] text-zinc-400">Myshop</p><p className="font-bold">Store administration</p></div><Link href="/" className="rounded-xl border border-zinc-200 px-4 py-2 text-sm font-bold hover:bg-zinc-50">View store</Link></header><main className="mx-auto max-w-[1500px] p-4 pb-24 sm:p-6 md:p-8">{children}</main><nav className="fixed bottom-0 left-0 right-0 z-40 grid grid-cols-5 border-t bg-white/95 shadow-[0_-5px_20px_rgba(0,0,0,.06)] backdrop-blur md:hidden">{links.slice(0,5).map(([href,label,Icon])=><Link key={href} href={href} className="flex flex-col items-center gap-1 py-3 text-[10px] font-bold text-zinc-500 hover:text-zinc-950"><Icon size={18}/>{label}</Link>)}</nav></div>