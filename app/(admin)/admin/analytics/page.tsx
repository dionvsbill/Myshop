import { Activity, ShoppingCart, WalletCards } from "lucide-react";
import { createClient } from "../../../../lib/supabase/server";
export default async function Analytics(){const s=await createClient();const {data}=await s.from("orders").select("total,status,created_at").order("created_at",{ascending:false}).limit(100);const active=(data||[]).filter(x=>x.status!=="CANCELLED");const revenue=active.reduce((n,x)=>n+Number(x.total||0),0);const metrics=[{label:"Revenue",value:"GHS "+revenue.toFixed(2),Icon:WalletCards},{label:"Recent orders",value:String(data?.length||0),Icon:ShoppingCart},{label:"Active orders",value:String(active.length),Icon:Activity}];return <section><p className="eyebrow">Reports</p><h1 className="page-title mt-2">Analytics</h1><p className="mt-2 text-sm text-neutral-500">A clear view of recent store performance.</p><div className="mt-7 grid gap-4 md:grid-cols-3">{metrics.map(({label,value,Icon})=><div key={label} className="store-card p-6"><div className="flex items-center justify-between"><p className="text-sm text-neutral-500">{label}</p><Icon size={18} className="text-neutral-400"/></div><p className="mt-4 text-3xl font-black tracking-tight">{value}</p></div>)}</div><div className="store-card mt-6 p-6"><h2 className="font-bold">Recent order activity</h2><div className="mt-5 space-y-3">{(data||[]).slice(0,8).map((x,i)=><div key={i} className="flex flex-wrap items-center justify-between gap-3 border-b pb-3 text-sm last:border-0"><span className="text-neutral-500">{new Date(x.created_at).toLocaleDateString()}</span><span className="font-semibold">{x.status}</span><span>GHS {Number(x.total).toFixed(2)}</span></div>)}</div></div></section>}