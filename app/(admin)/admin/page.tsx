import {createClient} from "@/lib/supabase/server"; export default async function Admin(){const s=await createClient();const [{count:products},{count:users},{count:orders},{data:rev}]=await Promise.all([s.from("products").select("id",{count:"exact",head:true}),s.from("profiles").select("id",{count:"exact",head:true}),s.from("orders").select("id",{count:"exact",head:true}),s.from("orders").select("total").neq("status","CANCELLED")]);const revenue=(rev||[]).reduce((n,x)=>n+Number(x.total||0),0);return <section><h2 className="text-2xl font-bold">Dashboard</h2><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[["Products",products],["Users",users],["Orders",orders],["Revenue","GHS "+revenue.toFixed(2)]].map(x=><div key={String(x[0])} className="rounded-xl border bg-white p-5 shadow-sm"><p className="text-sm text-slate-500">{x[0]}</p><p className="mt-2 text-2xl font-bold">{x[1]}</p></div>)}</div></section>}