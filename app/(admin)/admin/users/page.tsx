import {createClient} from "@/lib/supabase/server"; export default async function Users(){const s=await createClient();const {data}=await s.from("profiles").select("id,email,full_name,role,phone,created_at").order("created_at",{ascending:false});return <section><h2 className="text-2xl font-bold">Users</h2><div className="mt-6 overflow-x-auto rounded-xl border bg-white"><table className="w-full text-left text-sm"><thead className="border-b bg-slate-50"><tr><th className="p-4">Email</th><th>Name</th><th>Phone</th><th>Role</th></tr></thead><tbody>{(data||[]).map(u=><tr key={u.id} className="border-b"><td className="p-4">{u.email}</td><td>{u.full_name||"—"}</td><td>{u.phone||"—"}</td><td>{u.role}</td></tr>)}</tbody></table></div></section>}