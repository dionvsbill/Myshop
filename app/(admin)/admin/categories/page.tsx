import {createClient} from "../../../../lib/supabase/server"; export default async function Categories(){const s=await createClient();const {data}=await s.from("categories").select("*").order("name");return <section><h2 className="text-2xl font-bold">Categories</h2><div className="mt-6 rounded-xl border bg-white p-5"><div className="space-y-3">{(data||[]).map(c=><div key={c.id} className="flex justify-between border-b pb-3"><span>{c.name}</span><span className="text-slate-500">{c.slug}</span></div>)}</div></div></section>}