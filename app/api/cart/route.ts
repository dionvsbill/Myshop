import {NextResponse} from "next/server"; import {createClient} from "@/lib/supabase/server"; export async function POST(req:Request){const f=await req.formData();const product_id=String(f.get("product_id")||"");const quantity=Math.max(1,Number(f.get("quantity")||1));const s=await createClient();const {data:u}=await s.auth.getUser();if(!u.user)return NextResponse.redirect(new URL("/login",req.url));const {data:p}=await s.from("products").select("id,stock,is_active").eq("id",product_id).single();if(!p||!p.is_active||p.stock<quantity)return NextResponse.json({error:"Product unavailable"}, {status:400});const {error}=await s.from("cart_items").upsert({user_id:u.user.id,product_id,quantity},{onConflict:"user_id,product_id"});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.redirect(new URL("/cart",req.url))}