import {z} from "zod"; export const productSchema=z.object({title:z.string().min(2),slug:z.string().min(2),description:z.string().min(2),price:z.coerce.number().nonnegative(),compare_price:z.coerce.number().nonnegative().optional(),stock:z.coerce.number().int().nonnegative(),sku:z.string().optional(),category_id:z.string().uuid().optional(),is_featured:z.boolean().default(false),is_active:z.boolean().default(true)}); export const checkoutSchema=z.object({full_name:z.string().min(2),phone:z.string().min(7),address:z.string().min(3),city:z.string().min(2),region:z.string().min(2)});